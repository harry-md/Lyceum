info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/api/v1/auth/login
  body:
    type: json
    data: |-
      {
        "identifier": "user1",
        "password": "1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
